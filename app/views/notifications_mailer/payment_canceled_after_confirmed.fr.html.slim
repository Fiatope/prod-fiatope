= content_for :title, t('.titre')
- if @notification.contribution.present?
  - resource = @notification.contribution
- else
  - resource = @notification.match

|Projet: #{resource.project.name}
br/
|Key: 
strong #{resource.key}
br/
|Numéro du Paiement #: 
strong #{resource.payment_id}
br/
|Méthode de Paiement: 
strong #{resource.payment_method} 
br/
br/
|Le paiement #{resource.class.model_name.human} a été annulé par le contributeur ou la banque.
br/
|Vérifier le statut de la transaction sur la plateforme

